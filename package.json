{
  "name": "csv-image-processing",
  "version": "0.0.1",
  "description": "CSV Image Processing",
  "author": "Chirag Jain",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "graph": "nx graph",
    "start:dev": "lerna run start:dev --scope app --stream",
    "test": "lerna run test --scope app --stream",
    "test:cov": "lerna run test:cov --scope app --stream"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "jest": "28.0.3",
    "jest-junit": "14.0.1",
    "lerna": "8.1.7",
    "nx": "19.5.6",
    "prettier": "3.0.0",
    "ts-jest": "28.0.1",
    "ts-loader": "9.5.2",
    "ts-node": "10.9.2",
    "typescript": "4.9.5"
  },
  "workspaces": [
    "app",
    "packages/*"
  ],
  "packageManager": "yarn@4.3.1"
}
